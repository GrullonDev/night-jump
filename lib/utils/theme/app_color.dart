import 'package:flutter/material.dart';

class AppColor {
  AppColor._();

  // ── Surface Colors ──
  static const Color surface = Color(0xFF051424);
  static const Color surfaceDim = Color(0xFF051424);
  static const Color surfaceBright = Color(0xFF2C3A4C);
  static const Color surfaceContainerLowest = Color(0xFF010F1F);
  static const Color surfaceContainerLow = Color(0xFF0D1C2D);
  static const Color surfaceContainer = Color(0xFF122131);
  static const Color surfaceContainerHigh = Color(0xFF1C2B3C);
  static const Color surfaceContainerHighest = Color(0xFF273647);
  static const Color surfaceVariant = Color(0xFF273647);
  static const Color surfaceTint = Color(0xFF00DBE9);

  // ── Primary Colors ──
  static const Color primary = Color(0xFFDBFCFF);
  static const Color onPrimary = Color(0xFF00363A);
  static const Color primaryContainer = Color(0xFF00F0FF);
  static const Color onPrimaryContainer = Color(0xFF006970);
  static const Color inversePrimary = Color(0xFF006970);

  // ── Primary Fixed ──
  static const Color primaryFixed = Color(0xFF7DF4FF);
  static const Color primaryFixedDim = Color(0xFF00DBE9);
  static const Color onPrimaryFixed = Color(0xFF002022);
  static const Color onPrimaryFixedVariant = Color(0xFF004F54);

  // ── Secondary Colors ──
  static const Color secondary = Color(0xFFFFB1C4);
  static const Color onSecondary = Color(0xFF65002E);
  static const Color secondaryContainer = Color(0xFFFF4A8D);
  static const Color onSecondaryContainer = Color(0xFF590028);

  // ── Secondary Fixed ──
  static const Color secondaryFixed = Color(0xFFFFD9E1);
  static const Color secondaryFixedDim = Color(0xFFFFB1C4);
  static const Color onSecondaryFixed = Color(0xFF3F001A);
  static const Color onSecondaryFixedVariant = Color(0xFF8F0044);

  // ── Tertiary Colors ──
  static const Color tertiary = Color(0xFFFFF3F4);
  static const Color onTertiary = Color(0xFF640034);
  static const Color tertiaryContainer = Color(0xFFFFCCDA);
  static const Color onTertiaryContainer = Color(0xFFB90066);

  // ── Tertiary Fixed ──
  static const Color tertiaryFixed = Color(0xFFFFD9E3);
  static const Color tertiaryFixedDim = Color(0xFFFFB0C9);
  static const Color onTertiaryFixed = Color(0xFF3E001E);
  static const Color onTertiaryFixedVariant = Color(0xFF8E004C);

  // ── Error Colors ──
  static const Color error = Color(0xFFFFB4AB);
  static const Color onError = Color(0xFF690005);
  static const Color errorContainer = Color(0xFF93000A);
  static const Color onErrorContainer = Color(0xFFFFDAD6);

  // ── Background & On-Background ──
  static const Color background = Color(0xFF051424);
  static const Color onBackground = Color(0xFFD4E4FA);

  // ── On-Surface & Outline ──
  static const Color onSurface = Color(0xFFD4E4FA);
  static const Color onSurfaceVariant = Color(0xFFB9CACB);
  static const Color outline = Color(0xFF849495);
  static const Color outlineVariant = Color(0xFF3B494B);

  // ── Inverse Surface ──
  static const Color inverseSurface = Color(0xFFD4E4FA);
  static const Color inverseOnSurface = Color(0xFF233143);

  // ── Brand Colors (Design System) ──
  static const Color electricCyan = Color(0xFF00F0FF);
  static const Color intenseMagenta = Color(0xFFFF007F);
  static const Color neonRose = Color(0xFFFF2E93);
  static const Color slateWhite = Color(0xFFE2E8F0);
  static const Color slateGlow = Color(0xFF94A3B8);

  // ── Canvas & Surface Deep ──
  static const Color canvasBase = Color(0xFF020308);
  static const Color canvasMidnight = Color(0xFF050813);
  static const Color hudGlass = Color(0xFF090E1E);
  static const Color interactiveOverlay = Color(0xFF070A18);
  static const Color inputRecessed = Color(0xFF040712);
  static const Color deepNavy = Color(0xFF0B1226);
  static const Color translucentDarkSlate = Color(0xFF070B1A);

  // ── Shield & Gem Colors ──
  static const Color shieldCyan = Color(0xFF00F0FF);
  static const Color gemGreen = Color(0xFF39FF14);
  static const Color gemGreenDark = Color(0xFF1A8A0A);
}
